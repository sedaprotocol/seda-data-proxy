name: 🧹 Git Checks
on: [pull_request]

permissions: {}

jobs:
  block_fixup:
    name: "Block Fixup Commit Merge"
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: 🛃 Checkout repository
        uses: actions/checkout@v4

      - name: 💂 Block Fixup Commit Merge
        uses: sedaprotocol/block-fixup-merge-action@v1.0.0
